import { PwdValidateDirective } from './pwd-validate.directive';

describe('PwdValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdValidateDirective();
    expect(directive).toBeTruthy();
  });
});
